apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "porter-agent.fullname" . }}-test-connection"
  labels:
    {{- include "porter-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "porter-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
